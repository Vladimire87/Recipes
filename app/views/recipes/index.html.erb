<%= render 'search_form' %>
<%= link_to "Create New Recipe", new_recipe_path, class: "btn btn-primary mb-3" %>
<% @recipes.each do |recipe| %>
  <div class="recipe">
    <h4><%= link_to recipe.title, recipe_path(recipe) %></h4>
    <p><%= recipe.description %></p>
  </div>
<% end %>
<%== pagy_bootstrap_nav(@pagy) %>
